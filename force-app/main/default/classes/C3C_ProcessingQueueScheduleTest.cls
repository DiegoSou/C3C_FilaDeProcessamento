@isTest
public with sharing class C3C_ProcessingQueueScheduleTest {
    @isTest
    public static void executeTest() {
        //GIVEN
        //WHEN
        new C3C_ProcessingQueueSchedule().execute(null);
        //THEN
    }
}
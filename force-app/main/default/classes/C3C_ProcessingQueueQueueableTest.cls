@isTest
public with sharing class C3C_ProcessingQueueQueueableTest {
    @isTest
    public static void executeTest() {
        //GIVEN
        //WHEN
        new C3C_ProcessingQueueQueueable(null).execute(null);
        //THEN
    }
}